+ load edges
    + ensure db is empty before loading edges to the database
+ load nodes
    + ensure db is empty before loading nodes to the database
+ submitting single query through public function
+ inspecting current database numerical information

+ database read/write operation should be idempotent because the driver can work in a way that
retry the given transaction function in case of failed commit attempt
+ give feedback for uploading custom dataset loading into db
